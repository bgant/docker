# start
docker-compose up --scale app=4 
<scale should be <= CPU cores?>

# start in background
docker-compose up -d --scale app=4

# test from another terminal
wget -O - http://localhost:1313
<each time it is run the os name should change>

# stop
docker-compose down
